# =============================================================================
# NE JAMAIS COMMITTER LE FICHIER .env (copie de ce fichier avec les vraies clés).
# Ce fichier (env.example) peut rester public : il ne contient pas de secrets.
# =============================================================================

# Supabase (obligatoire) – l'anon key est publique par design ; la sécurité vient des RLS
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Google OAuth – utilisés uniquement par Supabase (serveur), jamais exposés au front
# À créer : https://console.cloud.google.com/apis/credentials
# Redirect URI : https://<PROJECT_REF>.supabase.co/auth/v1/callback
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
