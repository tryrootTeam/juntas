<template>
  <div class="grid grid-cols-1 sm:grid-cols-3 gap-4">
    <div class="bg-white rounded-xl border border-cream-400 shadow-sm p-5 flex items-center gap-4">
      <div class="flex-shrink-0 w-12 h-12 rounded-full bg-sage/20 flex items-center justify-center text-sage">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z" />
        </svg>
      </div>
      <div>
        <p class="text-sm font-medium text-deepblue-300">Matches totales</p>
        <p class="text-2xl font-bold text-deepblue">{{ stats.totalMatches ?? 0 }}</p>
      </div>
    </div>

    <div class="bg-white rounded-xl border border-cream-400 shadow-sm p-5 flex items-center gap-4">
      <div class="flex-shrink-0 w-12 h-12 rounded-full bg-terracota/20 flex items-center justify-center text-terracota">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z" />
        </svg>
      </div>
      <div>
        <p class="text-sm font-medium text-deepblue-300">Conversaciones activas</p>
        <p class="text-2xl font-bold text-deepblue">{{ stats.activeConversations ?? 0 }}</p>
      </div>
    </div>

    <div class="bg-white rounded-xl border border-cream-400 shadow-sm p-5 flex items-center gap-4">
      <div class="flex-shrink-0 w-12 h-12 rounded-full bg-deepblue/10 flex items-center justify-center text-deepblue">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z" />
        </svg>
      </div>
      <div>
        <p class="text-sm font-medium text-deepblue-300">Compatibilidad media</p>
        <p class="text-2xl font-bold text-deepblue">{{ formattedAvg }}</p>
      </div>
    </div>
  </div>
</template>

<script setup>
import { computed } from 'vue'

const props = defineProps({
  stats: {
    type: Object,
    default: () => ({}),
  },
})

const formattedAvg = computed(() => {
  const v = props.stats.avgCompatibility ?? props.stats.avg_compatibility
  if (v == null) return '—'
  const n = Number(v)
  return Number.isFinite(n) ? `${Math.round(n)}%` : '—'
})
</script>
